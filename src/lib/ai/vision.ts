/**
 * Stub para detección de ingredientes desde imágenes.
 * Fase 2: integrar con Anthropic API (modelo Haiku).
 */
export async function detectIngredientsFromImage(
  _imageBase64: string,
): Promise<string[]> {
  return [];
}
